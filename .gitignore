# Root ignores for the PHP dashboard and nested projects

# OS junk
.DS_Store
Thumbs.db

# Logs
*.log

# Local data (do not publish DBs)
data/*.db

# (removed) Node artifacts in nested project 'base/' — folder no longer used
